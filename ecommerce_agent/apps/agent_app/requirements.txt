# Bootstrap dependency for the self-contained Agent App artifact.
# pyproject.toml and uv.lock are packaged beside this file; app.yaml runs
# `uv run --frozen` so the exact verified runtime graph is preserved.
uv==0.9.7
