# This App uses the project-level uv.lock as its dependency source.
# Do not add packages here. If the App needs a runtime dependency,
# add it to pyproject.toml in the project root and run `uv sync`.
#
# The `uvicorn` and `fastapi` start commands are configured in app.yaml;
# their packages are already resolved in uv.lock.
